- Revisit replacing `QualityBreakdown.weights` dict with a typed `Weights` record and direct fixed-field JSON encoding (`alpha/beta/gamma/delta`). One run improved `quality_ratio` (to ~3.93) but regressed combined metric due form variance; could be worthwhile when paired with formation-loop optimizations and confirmed over repeated runs.
- Revisit precomputed perfect-owner maps in `compat_unique_perfect_assignments` only with stronger evidence. Multiple variants (full precompute and large-N-gated precompute) continued to regress combined metric on the current baseline despite occasional quality-side movement.
- Revisit `resolve_weights` fast-path when all four weights are explicitly present and `normalize_weights == False`. A trial showed a large `quality_ratio` gain (~4.0 -> ~3.8) but worse `form_ratio`; may still be valuable if applied only on quality path or combined with a form-side win.
  - Related formation bundle (pre-resolved weights + lightweight quality tuple API + deferred assignment conversion) produced strong samples (~2.28) but nearby unchanged reruns were close (~2.31); additional retries also landed in the elevated ~2.46–2.67 range, so keep this de-prioritized unless paired with a clear quality-side win.
  - Fresh rebuild-verified check on current baseline: deferring only `assigned_people`/`TeamResult` materialization until after prune improved `quality_ratio` (~2.18) but again regressed `form_ratio` (~0.87), so this remains de-prioritized as a net combined tradeoff.
- Revisit compat-specific direct coverage in unique-assignment score-matrix construction. It improved `quality_ratio` (~4.0 -> ~3.83) but hurt `form_ratio`; may be worth enabling selectively for quality-only workloads.
  - New confirmation on current baseline: direct Compat branching inside `coverage_for_person_and_task_skill` (exact-skill fold, Paper required hoist) again lowered `quality_ratio` (~2.28) but regressed `form_ratio` (~0.93). Keep this path scoped as **quality-only candidate**, not shared form+quality.
  - Additional negative data: gated variants (quality-only routing and large-task-only enablement in unique matrix paths) still regressed combined ratio after clean rebuilds; deprioritize this family unless paired with a separately proven form-side gain.
- Revisit branch-and-bound pruning in formation exact search with stronger/tighter bounds. A safe global bound improved `quality_ratio` but regressed `form_ratio` due pruning overhead; task-specific bounds or delayed-pruning heuristics may flip this.
  - Recent rerun signal: conservative remaining-depth multiplier prune (`adjusted_quality * ceiling^remaining_tasks`) produced one of the better form ratios (~1.53, run2 ~2.63) when bundled with one-pass mean accumulators, but quality stayed too high for keep (~2.35 combined).
  - De-prioritize heuristic candidate sorting (coverage-scored best-first order) on top of the prune stack for now: sorting overhead outweighed gains in recent tests.
- Revisit formation candidate-quality caching with a cheaper key (e.g. bitmask/int key instead of string signatures) **only alongside a broader search rewrite**. Multiple direct memo-key representation experiments did not beat the current best; carrying the people bitmask through recursion (`#(task_depth, people_mask)`) performed better than recomputing masks each state but still stayed in the mid-2.5 band alone.
  - Additional confirmation: compact key `#(remaining_task_count, people_mask)` with precomputed person-bit lookup also failed to improve end-to-end ratio in isolation.
- Revisit formation-side caching of team personality/social components with lower-cost team keys. Component-override caching improved quality-side ratios but did not pay off with current string-signature lookup overhead.
- Revisit JS fast-decode fallback for request payloads (`JSON.parse` + direct model construction + existing Gleam validation, fallback to codec on unsupported shape). Results were highly unstable across reruns (one strong outlier, multiple regressions in the mid-2.5+ band); de-prioritize unless paired with a clearly reproducible form-side structural win.
- De-prioritize decode/encode-only stacks (fast decode + fast encode + request/response caching). Repeated tests show inconsistent or negative end-to-end impact despite occasional local quality improvements; keep this path paused until core form run2 cost is reduced.
- Revisit a formation-only micro-bundle of (a) request-level social target lookup + (b) small-team `remove_people` specialization (`1/2/3` direct ID filters). One sample reached ~2.47 with form ratio ~1.67; still not a keep, but it appears to be one of the more stable form-side micro baselines to pair with a larger quality-side win.
- Port Python's optimized Compat assignment path (`_assign_task_skills_compat`) more directly into Gleam (or equivalent semantics-preserving strategy) instead of continued micro-tuning around current DP/search implementation.
  - Start with a compile-safe, incremental implementation of the common `max_skills_per_member == 1` case (task_count <= team_count), validating parity/checks at each helper step before benchmarking.
  - De-prioritize partial equal-partition greedy substitutions (even with rescue/zero guards): both a larger scaffold and a later richer priority/analysis tie-break variant passed parity but still regressed materially; if revisiting, port the full Python compat assignment stack (including its cache strategy and complete tie semantics), not an in-between subset.
    - Current-baseline reconfirmation: an additional large-N-only shortcut (`task_count >= 6` compat unique greedy allocator) also regressed after clean rebuilds; reinforces that piecemeal greedy patches are unlikely to beat the existing DP/specialized small-kernel stack.
- Revisit a stricter Compat equal-partition shortcut before DP: if each task has exactly one perfect scorer (`score >= 1.0`) and mapping is bijective, assign directly. One run reached ~2.197 combined (near current band but not a keep); combining this shortcut with iterative equal-partition DP reconstruction improved form-side ratio in one sample (~1.63) but still failed combined keep threshold.
  - Related variants to avoid unless radically redesigned: unique-positive mapping shortcut, earlier generic n=3 permutation fast path, endpoint-only `compat_overfull` override for equal partitions, and large-partition-only (`task_count >= 6`) perfect-bijection gating all regressed in repeated trials.
  - Updated note: a cheaper strict perfect-bijection precheck (`score >= 1.0` unique member per task, direct assignment score=1.0) remains one of the better quality-side levers. Recent cleaner stack (one-pass means + shortcut, without extra form bundles) reached ~2.26 with comparatively lower variance (`stability_cv` ~0.68), but still above keep threshold.
  - De-prioritize adding compat direct-coverage unique-matrix shortcuts on top of this path for now; repeated runs pushed quality variance up and regressed combined (~2.53 with quality ratio ~4.05).
  - Promising follow-up: a fully hardcoded Compat 3x3 solver using direct 9-cell matrix reads + 6 permutation products (no recursive DP bookkeeping) produced one of the better recent samples (~2.37 combined, quality ~3.29). Bundles with larger form-side specializations (typed-weights fast path and specialized uncapped 3-task full-allocation search) and with explicit-weights fast-path quality tuning still missed combined keep (~2.46–2.53), so further progress likely needs a deeper form run2 breakthrough.
  - Latest confirmation: standalone 3x3 kernel again improved form-side behavior (form ratio ~1.57, run2 ~2.57) but quality ratio rose (~3.74), landing ~2.42 combined; likely useful only when paired with a reliable quality-path win.
- Revisit one-pass `geometric_mean` / `weighted_geometric_mean` accumulators as a secondary quality micro only in bundles. Isolated runs tended to improve `quality_ratio` modestly (~3.7 band) but did not materially move `form_ratio`, leaving combined results in the mid-2.5 range.
  - New near-miss on current baseline: in `team_social_score` 3+ branch, guard explicit-social detection behind `compat_default == 0.0` and fast-return `1/team_size` when no explicit teammate prefs exist. Sample landed at ~1.3126 (`quality_ratio` ~2.14, `form_ratio` ~0.805). Still not a keep, but worth reproducibility reruns or a quality-side pairing.
  - De-prioritize JS `Math.log` FFI replacement for `safe_log` for now. It occasionally showed strong quality-side drops but repeated reruns with/without companion micros were unstable and often regressed combined metric via form drift.
- Revisit formation-path `calculate_team_quality` lightweight summary API (returning `quality/social/delta/assignments` tuple to avoid per-candidate `QualityBreakdown.weights` dict construction) potentially bundled with a true branch-count reducer. In isolation and with compact task-count memo keying it improved local ratios (~2.41–2.50 combined) but still stayed above keep threshold.
- Revisit task-specific exact prune bounds in formation search: precomputing per-task adjusted-quality upper bounds over the full people pool and pruning by `adjusted_quality * product(remaining_upper_bounds)` produced one of the stronger recent structural samples (~2.35 combined, quality ~3.29, form ~1.68). Main follow-up is reducing precompute overhead and/or reusing those upper-bound scoring results during actual search.
  - Related near-best bundle to keep in reserve: one-pass geometric means + strict compat perfect-bijection shortcut + small-team remove specialization repeatedly lands in the low-2.2 to low-2.4 band (best recent ~2.217, form ~1.41) but remains variance-sensitive and has not crossed keep threshold.
  - New near-best variant: adding a hardcoded Compat 3x3 unique-assignment kernel (6 permutations over direct 9-cell scores) on top of the above bundle reached ~2.184 combined with improved stability (`quality_ratio` ~3.18, `form_ratio` ~1.50, `stability_cv` ~0.60). Still above keep threshold, but this is currently one of the strongest deterministic-looking stacks to re-test with one extra quality-side reducer.
  - Even tighter near-miss: `perfect-bijection + 3x3 kernel + small-team remove` **without** one-pass mean refactor hit ~2.1289 combined (`quality_ratio` ~3.13, `form_ratio` ~1.45), missing keep threshold by ~0.0003. Highest-priority candidate for immediate reproducibility reruns and minimal-delta follow-ups.
  - Major update: this line of small-team compat specializations paid off significantly when combined with formation prune/deferred-remove and decoder optimizations. Added 2x2 compat unique kernel and tiny-team task-preference fast paths now pushed combined ratio to ~1.30 band; keep extending only where low-complexity, high-frequency team sizes (2/3) are proven in benchmark workloads.
